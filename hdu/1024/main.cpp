#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
#include <vector>
#include <map>
#include <utility>
#include <queue>
#include <stack>
using namespace std;
const int INF=1<<30;
const double eps=1e-6;
const int N = 1000010;

int a[N],n,m;
vector<int> pos,neg;

void run()
{
    pos.clear();
    neg.clear();
    for(int i=0;i<n;i++)
    {
        scanf("%d",&x);
    }
}

int main()
{
//    freopen("case.txt","r",stdin);
    while(scanf("%d%d",&m,&n)!=EOF)
        run();
    return 0;
}
